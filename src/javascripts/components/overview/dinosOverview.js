// import utils from '../../helpers/utils';
// import dinoData from '../../helpers/data/dinoData';

const getUnderstaffedDinosList = () => {
  // see what shifts have less than two people watching a dinosaur
  // print the shifts that need to add more staff
};

export default { getUnderstaffedDinosList };
